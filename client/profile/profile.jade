template(name='profile')
    .container
        .ui.segment
            .ui.header Messages
            +messageList
        .ui.segment
            .ui.header Your Upvoted Tag Cloud
            // #generatePersonalCloud.ui.icon.button
                // i.refresh.icon
            // if authoredCloud
            //     .ui.segment
            //         .ui.header Authored Cloud
            //         each authoredCloud
            //             .ui.button
            //                 |#{this.name}
            //                 small #{this.count}
            each user.upvotedCloud
                .ui.large.basic.label(class=upvotedCloudTagClass)
                    |#{this.name}
                    small #{this.count}
            // if downvotedCloud
            //     .ui.header Downvoted Cloud
            //     each downvotedCloud
            //         .ui.label
            //             |#{this.name}
            //             small #{this.count}
        .ui.segment
            .ui.header Top Matched User Cloud
            .ui.divided.list
                each upVotedMatchCloud
                    .item
                        .ui.header #{this.matchedUser}
                            small (Common Point Count: #{this.totalCount})
                        each this.cloudIntersection
                            .ui.basic.label #{this.tag} #{this.min}
            // .ui.header Top Matched User List
            // each matchedUsersList
            //     .ui.divided.list
            //         .item
            //             strong #{this.matchedUser} (#{this.length}):
            //             each this.cloudIntersection
            //                 .ui.basic.label #{this}
                // each people
                //     .item
                //         |#{this.username}
                //         .ui.button.matchTwoUsersAuthoredCloud Calculate Authored Match
                //         .ui.button.matchTwoUsersUpvotedCloud Calculate Upvoted Match
                // .ui.header Authored Docs Tag Cloud Matches
                // each authoredCloudMatches
                //     .ui.divided.list
                //         .item
                //             | #{this.username}
                //             each userMatchAuthoredCloud
                //                 .ui.button
                //                     |#{this.name}
                //                     small #{this.count}
                // .ui.header Upvoted Docs Tag Cloud Matches
                // each upVotedCloudMatches
                //     .ui.divided.list
                //         .item
                //             | #{this.username}
                //             each userMatchUpvotedCloud
                //                 .ui.button
                //                     |#{this.name}
                //                     small #{this.count}
